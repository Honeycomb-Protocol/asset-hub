/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import {
  ControlledMerkleTrees,
  controlledMerkleTreesBeet,
} from '../types/ControlledMerkleTrees'
import { ResourceKind, resourceKindBeet } from '../types/ResourceKind'

/**
 * Arguments used to create {@link Resource}
 * @category Accounts
 * @category generated
 */
export type ResourceArgs = {
  bump: number
  project: web3.PublicKey
  mint: web3.PublicKey
  merkleTrees: ControlledMerkleTrees
  kind: ResourceKind
}

export const resourceDiscriminator = [10, 160, 2, 1, 42, 207, 51, 212]
/**
 * Holds the data for the {@link Resource} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Resource implements ResourceArgs {
  private constructor(
    readonly bump: number,
    readonly project: web3.PublicKey,
    readonly mint: web3.PublicKey,
    readonly merkleTrees: ControlledMerkleTrees,
    readonly kind: ResourceKind
  ) {}

  /**
   * Creates a {@link Resource} instance from the provided args.
   */
  static fromArgs(args: ResourceArgs) {
    return new Resource(
      args.bump,
      args.project,
      args.mint,
      args.merkleTrees,
      args.kind
    )
  }

  /**
   * Deserializes the {@link Resource} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [Resource, number] {
    return Resource.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Resource} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<Resource> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find Resource account at ${address}`)
    }
    return Resource.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'Assetw8uxLogzVXic5P8wGYpVdesS1oZHfSnBFHAu42s'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, resourceBeet)
  }

  /**
   * Deserializes the {@link Resource} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Resource, number] {
    return resourceBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link Resource} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return resourceBeet.serialize({
      accountDiscriminator: resourceDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Resource} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: ResourceArgs) {
    const instance = Resource.fromArgs(args)
    return resourceBeet.toFixedFromValue({
      accountDiscriminator: resourceDiscriminator,
      ...instance,
    }).byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Resource} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: ResourceArgs,
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      Resource.byteSize(args),
      commitment
    )
  }

  /**
   * Returns a readable version of {@link Resource} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      project: this.project.toBase58(),
      mint: this.mint.toBase58(),
      merkleTrees: this.merkleTrees,
      kind: this.kind.__kind,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const resourceBeet = new beet.FixableBeetStruct<
  Resource,
  ResourceArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['bump', beet.u8],
    ['project', beetSolana.publicKey],
    ['mint', beetSolana.publicKey],
    ['merkleTrees', controlledMerkleTreesBeet],
    ['kind', resourceKindBeet],
  ],
  Resource.fromArgs,
  'Resource'
)
